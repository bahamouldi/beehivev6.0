apiVersion: v1
kind: Secret
metadata:
  name: beewaf-tls-secret
type: kubernetes.io/tls
data:
  # Ces valeurs seront générées par la commande:
  # kubectl create secret tls beewaf-tls-secret --cert=tls/tls.crt --key=tls/tls.key
  # Ou utiliser cert-manager en production
  tls.crt: |
    # Base64 encoded certificate
  tls.key: |
    # Base64 encoded private key
