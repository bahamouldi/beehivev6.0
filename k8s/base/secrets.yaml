# =============================================================================
# BeeWAF — Kubernetes Secret (Base)
# =============================================================================
# NOTE: In production, use SealedSecrets or external secrets manager
# This is a template - do not commit real secrets!
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: beewaf-secrets
  labels:
    app.kubernetes.io/name: beewaf
type: Opaque
stringData:
  # Change this key in production!
  BEEWAF_API_KEY: "supersecret-beewaf-admin-key-2026"
